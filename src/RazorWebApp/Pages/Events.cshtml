@page
@model EventsModel

<h1>Upcoming Events</h1>

<p>Fill out the following form to tell us information about your upcoming event.</p>
@if(Model.RegisteredEvent != null){
    <p>Thank your for telling us about the <i>@Model.RegisteredEvent.Title</i> event. We will post an announcement of the event until the start date of <b>@Model.RegisteredEvent.Date.ToShortDateString()</b>.</p>
}

<form method="POST">
    <label>Title
        <input type="text" placeholder="Event Title" asp-for="Event.Title" />
    </label>
    <label>Description
        <textarea cols="30" rows="10" asp-for="Event.Description"></textarea>
    </label>
    <label>Date
        <input type="date" asp-for="Event.Date"/>
    </label>
    <label>Seats
        <input type="number" asp-for="Event.AvailableSeats" />
    </label>
    <button type="submit">Post Details</button>
</form>
